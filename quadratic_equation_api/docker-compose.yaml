version: '3'

services:
  api:
    build: ./
    container_name: api
    environment:
      - PYTHON_ENV=prod
    volumes:
      - "./:/usr/src/app"
    working_dir: "/usr/src/app"
    ports:
      - "8003:8000"
  